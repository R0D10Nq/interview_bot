# 🤖 Interview Notification Bot

Телеграм-бот для управления собеседованиями с автоматическими уведомлениями.

## 📋 Возможности

- ✅ Добавление информации о собеседованиях (компания, позиция, дата, платформа и т.д.)
- 🔔 Автоматические уведомления за 24ч, 12ч, 6ч, 3ч, 1.5ч, 30мин до интервью
- ⚙️ Настройка времени уведомлений
- 📊 Просмотр списка предстоящих интервью
- 🗑 Удаление интервью
- 📱 Удобный интерфейс с кнопками

## 🚀 Установка и запуск

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/R0D10Nq/interview_bot.git
cd interview_bot
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте конфигурацию

Создайте файл `.env` на основе `.env.example`:

```env
BOT_TOKEN=your_bot_token_from_@BotFather
DATABASE_URL=sqlite+aiosqlite:///./interviews.db
TZ=Europe/Moscow
```

### 5. Запустите бота

```bash
python -m app.main
```

## 🏗 Архитектура

Проект следует принципам **SOLID**, **Clean Architecture** и **PEP 8**:

```
app/
├── main.py                 # Точка входа
├── config.py              # Конфигурация
├── database/              # Слой данных
│   ├── database.py        # Подключение к БД
│   ├── models.py          # SQLAlchemy модели
│   └── repositories.py    # Репозитории (Data Access Layer)
├── handlers/              # Обработчики событий
│   ├── start.py
│   ├── interview.py
│   └── notifications.py
├── services/              # Бизнес-логика
│   ├── interview_service.py
│   └── notification_service.py
├── keyboards/             # UI компоненты
│   └── inline.py
├── states/                # FSM состояния
│   └── interview_states.py
└── utils/                 # Утилиты
    └── validators.py
```

## 🎯 Использование

### Добавление интервью

1. Нажмите "➕ Добавить интервью"
2. Последовательно введите:
   - Название компании
   - Позицию
   - Ссылку на вакансию (опционально)
   - Имя рекрутера
   - Дату и время (формат: ДД.ММ.ГГГГ ЧЧ:ММ)
   - Название платформы
   - Ссылку на встречу (опционально)
   - Требуется ли камера
   - Тип интервью
3. Подтвердите данные

### Настройка уведомлений

1. Откройте "⚙️ Настройки уведомлений"
2. Можно:
   - Включить/выключить уведомления
   - Изменить время уведомлений (введите часы через запятую)
   - Сбросить на стандартные настройки

## 📦 Технологии

- **Python 3.10+**
- **aiogram 3.4** - async Telegram Bot API framework
- **SQLAlchemy 2.0** - ORM
- **SQLite** - база данных
- **APScheduler** - планировщик задач
- **Pydantic** - валидация данных

## 🔧 Разработка

### Принципы

- **SOLID** - разделение ответственности
- **DDD** - доменная модель
- **Repository Pattern** - абстракция данных
- **FSM** - управление состояниями
- **Dependency Injection** - внедрение зависимостей
- **Type Hints** - аннотации типов
- **PEP 8** - стиль кода

### Тестирование

```bash
# TODO: добавить тесты
pytest tests/
```

## 📝 Лицензия

MIT
